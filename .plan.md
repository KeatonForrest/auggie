# Auggie API & SDK — From B+ to "Best I've Ever Used"

## What separates good from legendary

The APIs developers rave about (Stripe, Resend, Vercel) share these traits:
1. **Nothing is broken** — every edge case is handled, every error is helpful
2. **The SDK feels like magic** — typed responses, autocomplete, zero surprises
3. **Trust signals everywhere** — rate limit headers, request IDs in errors, consistent envelope
4. **You never get double-charged** — bulletproof idempotency, atomic operations

Here's the plan to get there, organized in 4 phases.

---

## Phase 1: Fix What's Broken (critical bugs) ✅ DONE

All items completed and deployed:
- ~~1a. Fix webhook endpoints~~ — fixed `api_user["user_id"]` → `api_user["id"]`
- ~~1b. Fix CORS~~ — added PATCH, OPTIONS methods + idempotency/request-id headers
- ~~1c. Handle RequestValidationError~~ — normalized to error envelope format
- ~~1d. Schedule idempotency cleanup~~ — runs every 60min in worker poll loop
- ~~1e. Stop leaking internal errors~~ — generic message + server-side logging

---

## Phase 2: Reliability Hardening (prevent double-charging) ✅ DONE

All items completed and deployed:
- ~~2a. Fix idempotency race condition~~ — atomic INSERT ON CONFLICT claim, 409 for in-progress keys
- ~~2b. Make bulk credit deduction atomic~~ — `create_bulk_job_with_credit()` in single transaction
- ~~2c. Add retry backoff to task queue~~ — `run_at` column, exponential delay (10s * 2^attempts, cap 5min)
- ~~2d. Fix stale requeue incrementing~~ — requeue now increments attempts, respects max_attempts
- ~~2e. Add advisory lock to migration runner~~ — `pg_advisory_lock` prevents concurrent startup races

---

## Phase 3: Developer Experience Polish (the "wow" factor) ✅ DONE

All items completed and deployed:
- ~~3a. Rate limit headers~~ — `RateLimitHeaderMiddleware` already existed
- ~~3b. Request ID in error bodies~~ — already in all exception handlers
- ~~3c. SDK User-Agent header~~ — `auggie-python/{version}` on both clients
- ~~3d. SDK fix create_list default~~ — aligned `analyze=False` to match API
- ~~3e. SDK webhook verification~~ — `Webhook` class already existed
- ~~3f. SDK bulk_research_and_poll~~ — already existed
- ~~3g. Access logging middleware~~ — logs method/path/status/latency for API routes

---

## Phase 4: Production Operations ✅ DONE

All items completed and deployed:
- ~~4a. Credit audit trail~~ — `credit_ledger` table, entries on every charge/refund/topup
- ~~4b. Completed task cleanup~~ — hourly cleanup of tasks older than 7 days
- ~~4c. Pool monitoring~~ — `/health` returns pool size/free/used stats
