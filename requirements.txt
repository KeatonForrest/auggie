# FastAPI - the web framework
fastapi==0.115.0

# Uvicorn - ASGI server to run FastAPI
uvicorn==0.30.0

# Httpx - async HTTP client (like requests, but async)
httpx==0.27.0

# Pydantic settings - for loading environment variables
pydantic-settings==2.2.0

# Anthropic - Claude API client
anthropic>=0.49.0

# Python-dotenv - load .env files
python-dotenv==1.0.1

# Jinja2 - templating for HTML output
jinja2==3.1.6

# WeasyPrint - PDF generation
weasyprint==62.0

# Asyncpg - async PostgreSQL driver
asyncpg==0.30.0

# Technology detection (custom engine)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Async DNS resolution
aiodns>=3.0.0

# Stripe - payments
stripe==8.0.0

# Auth - Google OAuth
authlib==1.3.0
itsdangerous==2.1.2
PyJWT==2.8.0

# API key hashing
bcrypt==4.2.0

# Form handling
python-multipart==0.0.9

# Structured JSON logging (production)
python-json-logger>=2.0.0

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-mock==3.14.0

# ===========================================
# v2 Materials Feature Dependencies
# ===========================================

# OpenAI - for embeddings
openai>=1.0.0

# Cloudflare R2 storage (S3-compatible)
boto3>=1.34.0

# Document parsing
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=0.6.23

# pgvector support for asyncpg
pgvector>=0.2.0

# TTL caching for auth lookups
cachetools>=5.3.0

# Excel file parsing
openpyxl>=3.1.0
